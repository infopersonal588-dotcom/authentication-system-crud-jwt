{
  "name": "authentication-system-crud-jwt",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install-all": "npm install --workspaces",
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "start": "node server/server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
